name: Manual workflow
on: [ workflow_dispatch ]

jobs:
  test:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - uses: takost/write-args-action@main
      with:
        args: "hello"
    
